<#--
<jsp:useBean id="functions" scope="request" class="java.util.ArrayList"/>
<!-- 功能按钮列表 &ndash;&gt;
<c:if test="${!empty functions}">
	<blockquote class="layui-elem-quote layui-function-area">
		<c:forEach items="${functions}" var="item">
			<a href="javascript:void(0);" class="layui-btn layui-btn-sm" name="${item.name}" data-url="${item.url}">
				<i class="layui-icon">${item.icon}</i> ${item.description}
			</a>
		</c:forEach>
	</blockquote>
</c:if>-->
functions
